# 📚 Покрокова інструкція: Завантаження проекту на GitHub

## 📋 Підготовка

### 1. Встановіть Git (якщо ще не встановлено)

**Linux/Ubuntu:**
```bash
sudo apt update
sudo apt install git
```

**Windows:**
Завантажте з https://git-scm.com/download/win

**macOS:**
```bash
brew install git
```

### 2. Налаштуйте Git (якщо перший раз)

```bash
git config --global user.name "Ваше Ім'я"
git config --global user.email "your.email@example.com"
```

---

## 🌐 Створення репозиторію на GitHub

### Крок 1: Зареєструйтесь на GitHub
1. Перейдіть на https://github.com
2. Натисніть **Sign up**
3. Заповніть форму реєстрації

### Крок 2: Створіть новий репозиторій
1. Натисніть **+** у правому верхньому куті
2. Виберіть **New repository**
3. Заповніть поля:
   - **Repository name**: `SM4-Encryption-Utility` (або інша назва)
   - **Description**: `🔐 SM4 Encryption utility with bilingual UI (Ukrainian/English)`
   - **Public** або **Private** (на ваш вибір)
   - ❌ **НЕ** ставте галочки на:
     - Add a README file
     - Add .gitignore
     - Choose a license
   (ми вже створили ці файли локально)
4. Натисніть **Create repository**

---

## 💻 Завантаження проекту з командного рядка

### Крок 3: Ініціалізуйте Git у папці проекту

```bash
# Перейдіть у папку проекту
cd /home/r8s8/SM4

# Ініціалізуйте git репозиторій
git init

# Додайте всі файли
git add .

# Створіть перший коміт
git commit -m "🎉 Initial commit: SM4 Encryption Utility with bilingual UI"
```

### Крок 4: З'єднайте з GitHub

Замініть `YOUR_USERNAME` та `REPOSITORY_NAME` на ваші дані:

```bash
# Додайте віддалений репозиторій
git remote add origin https://github.com/YOUR_USERNAME/SM4-Encryption-Utility.git

# Перейменуйте гілку на main (якщо потрібно)
git branch -M main

# Завантажте код на GitHub
git push -u origin main
```

### Крок 5: Введіть облікові дані
При першому push GitHub попросить:
- **Username**: ваш GitHub username
- **Password**: використовуйте **Personal Access Token** (не звичайний пароль!)

#### Як створити Personal Access Token:
1. На GitHub: Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Натисніть **Generate new token (classic)**
3. Дайте назву, наприклад: "SM4 Project"
4. Виберіть `repo` (повний доступ)
5. Натисніть **Generate token**
6. **ВАЖЛИВО**: Скопіюйте і збережіть токен! Він показується тільки раз
7. Використовуйте цей токен замість пароля

---

## 📸 Додавання скріншотів (опціонально)

### Крок 6: Створіть папку для скріншотів

```bash
mkdir -p screenshots
```

### Крок 7: Зробіть скріншоти програми
1. Запустіть програму
2. Зробіть скріншоти:
   - Головний інтерфейс
   - Режим "Текст"
   - Режим "Файли"
   - Перемикач мови
3. Збережіть їх у папку `screenshots/` з назвами:
   - `main-interface.png`
   - `text-mode.png`
   - `file-mode.png`
   - `language-switcher.png`

### Крок 8: Завантажте скріншоти

```bash
# Додайте скріншоти
git add screenshots/

# Зробіть коміт
git commit -m "📸 Add screenshots"

# Завантажте на GitHub
git push
```

---

## 🎨 Альтернатива: Завантаження через веб-інтерфейс

### Якщо командний рядок не підходить:

1. **Створіть репозиторій** на GitHub (як у Кроці 2)
2. **Відкрийте репозиторій** на GitHub
3. Натисніть **uploading an existing file**
4. **Перетягніть файли** з папки проекту
5. Напишіть коміт: `Initial commit`
6. Натисніть **Commit changes**

---

## 🔄 Подальша робота з проектом

### Внесення змін:

```bash
# 1. Внесіть зміни у код

# 2. Перевірте статус
git status

# 3. Додайте змінені файли
git add .

# 4. Зробіть коміт з описом
git commit -m "✨ Add new feature: описание"

# 5. Завантажте на GitHub
git push
```

### Корисні команди:

```bash
# Перегляд історії комітів
git log --oneline

# Перевірка віддалених репозиторіїв
git remote -v

# Завантаження змін з GitHub
git pull

# Перегляд різниці
git diff
```

---

## 📝 Редагування README

Якщо потрібно оновити README:

```bash
# 1. Відредагуйте файл README.md

# 2. Додайте зміни
git add README.md

# 3. Зробіть коміт
git commit -m "📝 Update README"

# 4. Завантажте
git push
```

---

## ✅ Перевірка

Після завантаження:
1. Відкрийте ваш репозиторій на GitHub
2. Переконайтесь, що всі файли завантажені
3. README.md повинен відображатися красиво відформатованим
4. Перевірте, чи працюють посилання

---

## 🎯 Корисні поради

### Емоджі у комітах:
- 🎉 `:tada:` - Initial commit
- ✨ `:sparkles:` - New feature
- 🐛 `:bug:` - Bug fix
- 📝 `:memo:` - Documentation
- 🎨 `:art:` - UI/Style
- ♻️ `:recycle:` - Refactoring
- 🔒 `:lock:` - Security
- 🌐 `:globe_with_meridians:` - Internationalization

### Структура комітів:
```
<тип>: <короткий опис>

<детальний опис (опціонально)>
```

Приклад:
```bash
git commit -m "✨ Add language switcher

- Add Ukrainian and English translations
- Implement dynamic UI rebuild
- Add language toggle button"
```

---

## 🆘 Проблеми та рішення

### Помилка: "remote origin already exists"
```bash
git remote remove origin
git remote add origin https://github.com/YOUR_USERNAME/REPO.git
```

### Помилка при push
```bash
# Спробуйте спочатку завантажити зміни
git pull origin main --allow-unrelated-histories
git push origin main
```

### Забули додати файл до .gitignore
```bash
# Видаліть файл з Git (але не з диску)
git rm --cached filename

# Додайте у .gitignore
echo "filename" >> .gitignore

# Закомітьте
git commit -m "🙈 Update .gitignore"
git push
```

---

## 🎓 Додаткові ресурси

- [GitHub Docs](https://docs.github.com)
- [Git Cheat Sheet](https://education.github.com/git-cheat-sheet-education.pdf)
- [Markdown Guide](https://www.markdownguide.org)
- [GitHub Profile README Generator](https://rahuldkjain.github.io/gh-profile-readme-generator/)

---

**Успіхів з вашим проектом на GitHub! 🚀**
