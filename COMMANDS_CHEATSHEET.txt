╔══════════════════════════════════════════════════════════════╗
║          🚀 ШВИДКА ШПАРГАЛКА GIT КОМАНД                      ║
╔══════════════════════════════════════════════════════════════╗

📤 ПЕРШЕ ЗАВАНТАЖЕННЯ
─────────────────────────────────────────────────────────────
cd /home/r8s8/SM4
git init
git add .
git commit -m "🎉 Initial commit"
git remote add origin https://github.com/YOUR_USERNAME/SM4-Encryption-Utility.git
git branch -M main
git push -u origin main

⚡ АБО ПРОСТО:
─────────────────────────────────────────────────────────────
./upload_to_github.sh


🔄 РЕГУЛЯРНІ ОНОВЛЕННЯ
─────────────────────────────────────────────────────────────
git status                              # Подивитися що змінилося
git add .                               # Додати всі зміни
git commit -m "✨ Опис змін"           # Зберегти
git push                                # Відправити на GitHub


📸 ДОДАТИ СКРІНШОТИ
─────────────────────────────────────────────────────────────
git add screenshots/
git commit -m "📸 Add screenshots"
git push


🔍 ПЕРЕВІРКА СТАТУСУ
─────────────────────────────────────────────────────────────
git status                              # Що змінилося
git log --oneline                       # Історія комітів
git remote -v                           # Віддалені репозиторії
git diff                                # Різниця в змінах


🎨 КОРИСНІ КОМІТИ
─────────────────────────────────────────────────────────────
git commit -m "✨ Add new feature"      # Нова функція
git commit -m "🐛 Fix bug"             # Виправлення
git commit -m "📝 Update docs"         # Документація
git commit -m "🎨 Improve UI"          # Інтерфейс
git commit -m "⚡ Optimize"            # Оптимізація
git commit -m "🔒 Security fix"        # Безпека
git commit -m "🌐 Add translations"    # Переклади


🆘 ПРОБЛЕМИ
─────────────────────────────────────────────────────────────
# Remote already exists:
git remote remove origin
git remote add origin https://github.com/USER/REPO.git

# Скинути останній коміт (зміни залишаться):
git reset --soft HEAD~1

# Форсований push (обережно!):
git push --force


📋 КОРИСНІ ALIAS (додайте у ~/.gitconfig)
─────────────────────────────────────────────────────────────
[alias]
    st = status
    ci = commit
    co = checkout
    br = branch
    lg = log --oneline --graph --all
    last = log -1 HEAD


🔑 PERSONAL ACCESS TOKEN
─────────────────────────────────────────────────────────────
1. GitHub.com → Settings → Developer settings
2. Personal access tokens → Tokens (classic)
3. Generate new token (classic)
4. Виберіть 'repo' (повний доступ)
5. Скопіюйте токен (показується раз!)
6. Використовуйте замість пароля при git push


📞 ДОДАТКОВІ РЕСУРСИ
─────────────────────────────────────────────────────────────
START_HERE.md       - Швидкий старт
GITHUB_GUIDE.md     - Детальна інструкція
QUICK_COMMANDS.md   - Розширений список команд
CHECKLIST.md        - Чеклист перед публікацією

╚══════════════════════════════════════════════════════════════╝
