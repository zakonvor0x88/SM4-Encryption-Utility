# 🚀 Швидкі команди для роботи з GitHub

## 📤 Перше завантаження на GitHub

```bash
# 1. Перейдіть у папку проекту
cd /home/r8s8/SM4

# 2. Ініціалізуйте Git
git init

# 3. Додайте всі файли
git add .

# 4. Створіть перший коміт
git commit -m "🎉 Initial commit: SM4 Encryption Utility"

# 5. Додайте віддалений репозиторій (ЗАМІНІТЬ YOUR_USERNAME!)
git remote add origin https://github.com/YOUR_USERNAME/SM4-Encryption-Utility.git

# 6. Перейменуйте гілку на main
git branch -M main

# 7. Завантажте на GitHub
git push -u origin main
```

---

## 🔄 Регулярні оновлення

### Після внесення змін у код:

```bash
# Перевірте які файли змінилися
git status

# Додайте всі змінені файли
git add .

# Або додайте конкретний файл
git add "sm4 gui.py"

# Зробіть коміт з описом
git commit -m "✨ Опис ваших змін"

# Завантажте на GitHub
git push
```

---

## 📸 Додавання скріншотів

```bash
# Додайте скріншоти у папку screenshots/
git add screenshots/

# Зробіть коміт
git commit -m "📸 Add application screenshots"

# Завантажте
git push
```

---

## 🎯 Приклади корисних комітів

```bash
# Нова функція
git commit -m "✨ Add dark mode support"

# Виправлення помилки
git commit -m "🐛 Fix encryption error with special characters"

# Оновлення документації
git commit -m "📝 Update README with installation guide"

# Покращення UI
git commit -m "🎨 Improve button layout and colors"

# Переклади
git commit -m "🌐 Add Spanish language support"

# Оптимізація
git commit -m "⚡ Optimize file encryption speed"
```

---

## 🔧 Корисні команди

```bash
# Перегляд історії комітів
git log --oneline --graph --all

# Скасування останнього коміту (зміни залишаться)
git reset --soft HEAD~1

# Перегляд різниці перед комітом
git diff

# Перевірка статусу
git status

# Завантаження змін з GitHub
git pull

# Клонування репозиторію
git clone https://github.com/YOUR_USERNAME/SM4-Encryption-Utility.git
```

---

## 💡 Поради

1. **Робіть коміти часто** - краще багато маленьких комітів, ніж один великий
2. **Пишіть зрозумілі описи** - щоб потім легко знайти потрібні зміни
3. **Використовуйте емоджі** - вони роблять історію комітів наочнішою
4. **Перевіряйте перед push** - `git status` і `git diff`
5. **Регулярно робіть push** - щоб не втратити зміни

---

## 🎨 Емоджі для комітів

| Емоджі | Код | Використання |
|--------|-----|--------------|
| 🎉 | `:tada:` | Початковий коміт |
| ✨ | `:sparkles:` | Нова функція |
| 🐛 | `:bug:` | Виправлення помилки |
| 📝 | `:memo:` | Документація |
| 🎨 | `:art:` | UI/Стиль |
| ⚡ | `:zap:` | Покращення продуктивності |
| 🔒 | `:lock:` | Безпека |
| 🌐 | `:globe_with_meridians:` | Інтернаціоналізація |
| ♻️ | `:recycle:` | Рефакторинг |
| 📸 | `:camera_flash:` | Скріншоти |
| 🔧 | `:wrench:` | Налаштування |
| 🚀 | `:rocket:` | Деплой |

---

**Успішної роботи з Git! 💪**
